function rightTriangle(base, height){
    let area = 1/2 * base * height
    return area
}







console.log(rightTriangle(2, 2) === 2);
console.log(rightTriangle(1, 1) === 0.5);

//Notice we did not have to convert 2 to an integer like in main.py